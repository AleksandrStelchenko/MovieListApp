import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 0,
    paddingVertical: 16,
  },
  castContainer: {
    gap: 16,
    paddingHorizontal: 16,
  },
});
