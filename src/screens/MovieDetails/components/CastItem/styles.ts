import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    height: 150,
    aspectRatio: 670 / 1005,
    borderRadius: 16,
  },
  text: {
    color: "white",
    textAlign: "center",
    paddingTop: 8,
  },
});
